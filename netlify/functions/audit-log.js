const { json, supabaseRequest, verifyUser } = require('./_nova');
const OWNER='novasparkcreative@gmail.com';
exports.handler=async event=>{if(event.httpMethod!=='POST')return json(405,{error:'METHOD_NOT_ALLOWED'});try{const user=await verifyUser(event.headers.authorization||event.headers.Authorization);if(!user)return json(401,{error:'AUTHENTICATION_REQUIRED'});const body=JSON.parse(event.body||'{}'),org=String(body.organization_id||'').trim();if(!org)return json(400,{error:'ORGANIZATION_ID_REQUIRED'});const allowed=Array.isArray(user.organization_ids)?user.organization_ids.includes(org):user.organization_id===org;if(!allowed||String(user.email||'').toLowerCase()!==OWNER)return json(403,{error:'OWNER_ACCESS_REQUIRED'});const limit=Math.min(Math.max(Number(body.limit)||50,1),100);const rows=await supabaseRequest(`events?organization_id=eq.${encodeURIComponent(org)}&select=id,event_type,source,user_id,payload,created_at&order=created_at.desc&limit=${limit}`);return json(200,{ok:true,events:Array.isArray(rows)?rows:[]})}catch(e){console.error(e);return json(500,{error:'AUDIT_LOG_FAILED'})}};