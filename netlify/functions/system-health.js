const { json, supabaseRequest, verifyUser } = require('./_nova');
const OWNER='novasparkcreative@gmail.com';
exports.handler=async event=>{if(event.httpMethod!=='POST')return json(405,{error:'METHOD_NOT_ALLOWED'});try{const user=await verifyUser(event.headers.authorization||event.headers.Authorization);if(!user)return json(401,{error:'AUTHENTICATION_REQUIRED'});const b=JSON.parse(event.body||'{}'),org=String(b.organization_id||'').trim();if(!org)return json(400,{error:'ORGANIZATION_ID_REQUIRED'});const allowed=Array.isArray(user.organization_ids)?user.organization_ids.includes(org):user.organization_id===org;if(!allowed||String(user.email||'').toLowerCase()!==OWNER)return json(403,{error:'OWNER_ACCESS_REQUIRED'});const controls=await supabaseRequest(`system_controls?organization_id=eq.${encodeURIComponent(org)}&select=emergency_stop&limit=1`);const events=await supabaseRequest(`events?organization_id=eq.${encodeURIComponent(org)}&select=event_type,created_at&order=created_at.desc&limit=10`);return json(200,{ok:true,health:{auth:'online',database:'reachable',emergency_stop:Boolean(controls?.[0]?.emergency_stop),recent_events:Array.isArray(events)?events:[]}})}catch(e){console.error(e);return json(500,{error:'SYSTEM_HEALTH_FAILED'})}};